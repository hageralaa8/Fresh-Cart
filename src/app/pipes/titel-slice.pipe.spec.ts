import { TitelSlicePipe } from './titel-slice.pipe';

describe('TitelSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new TitelSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
